# pgen 

A basic CLI tool to generate basic passwords.

## Usage

`pgen [OPTIONS]`

Supported options:
- -l, --length <LENGTH>
- -m, --max-length <MAX LENGTH>

## To-do
[] Add tests.
[] Add GitHub Actions CI.
[] Automate releases.

